<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="RingLoadingView">
        <!-- 圆环开始颜色 -->
        <attr name="rlv_start_color" format="color|reference" />
        <!-- 圆环结束颜色 -->
        <attr name="rlv_end_color" format="color|reference" />
        <!-- 圆环的边宽 -->
        <attr name="rlv_ring_width" format="dimension|reference|float" />
        <!-- 是否自动开启旋转，默认开启 -->
        <attr name="rlv_auto_start" format="boolean" />
    </declare-styleable>

    <declare-styleable name="RotateDotView">
        <!-- 开始颜色 -->
        <attr name="rdv_start_color" format="color|reference" />
        <!-- 结束颜色 -->
        <attr name="rdv_end_color" format="color|reference" />
        <!-- 圆点数量 -->
        <attr name="rdv_dot_count" format="integer|dimension" />
        <!-- 圆点大小 -->
        <attr name="rdv_dot_radius" format="integer|float|dimension" />
        <!-- 是否自动开启旋转，默认开启 -->
        <attr name="rdv_auto_start" format="boolean" />
        <!-- 每次旋转的角度 -->
        <attr name="rdv_rotate_angle" format="integer|dimension" />
        <!-- 点模式，普通模式、缩放模式 -->
        <attr name="rdv_dot_mode" format="enum">
            <enum name="normal" value="1" />
            <enum name="scale" value="2" />
        </attr>
    </declare-styleable>

    <declare-styleable name="BackArrowView">
        <!-- 箭头颜色 -->
        <attr name="bav_color" format="color" />
        <!-- 箭头线宽 -->
        <attr name="bav_stroke_width" format="integer|float|dimension" />
        <!-- 模式 -->
        <attr name="bav_arrow_style" format="enum">
            <!-- Google Material Design -->
            <enum name="material_design" value="1" />
            <!-- 微信风格 -->
            <enum name="wechat_design" value="2" />
        </attr>
    </declare-styleable>

    <declare-styleable name="MoreActionView">
        <!-- 点的颜色 -->
        <attr name="mav_color" format="color" />
        <!-- 点大小 -->
        <attr name="mav_dot_radius" format="integer|float|dimension" />
        <!-- 排列方向，水平或垂直 -->
        <attr name="mav_orientation" format="enum">
            <enum name="horizontal" value="1" />
            <enum name="vertical" value="2" />
        </attr>
    </declare-styleable>

    <declare-styleable name="AndroidMenuView">
        <!-- 线的颜色 -->
        <attr name="amv_color" format="color" />
        <!-- 线高 -->
        <attr name="amv_line_height" format="integer|float|dimension" />
    </declare-styleable>

    <declare-styleable name="CloseView">
        <!-- 线的颜色 -->
        <attr name="cv_color" format="color" />
        <!-- 线高 -->
        <attr name="cv_line_width" format="integer|float|dimension" />
        <!-- 模式，普通模式、圆边模式 -->
        <attr name="cv_mode" format="enum">
            <enum name="normal" value="1" />
            <enum name="circle" value="2" />
        </attr>
        <!-- 圆的边颜色 -->
        <attr name="cv_circle_color" format="color" />
        <!-- 圆的边线宽 -->
        <attr name="cv_circle_line_width" format="integer|float|dimension" />
    </declare-styleable>

    <declare-styleable name="AddView">
        <!-- 线的颜色 -->
        <attr name="av_color" format="color" />
        <!-- 线高 -->
        <attr name="av_line_width" format="integer|float|dimension" />
        <!-- 模式，普通模式、圆边模式 -->
        <attr name="av_mode" format="enum">
            <enum name="normal" value="1" />
            <enum name="circle" value="2" />
        </attr>
        <!-- 圆的边颜色 -->
        <attr name="av_circle_color" format="color" />
        <!-- 圆的边线宽 -->
        <attr name="av_circle_line_width" format="integer|float|dimension" />
    </declare-styleable>

    <declare-styleable name="AndroidSearchView">
        <!-- 线的颜色 -->
        <attr name="asv_color" format="color" />
        <!-- 线宽 -->
        <attr name="asv_line_width" format="integer|float|dimension" />
    </declare-styleable>

    <declare-styleable name="CloudMusicLoadingView">
        <!-- 音轨数量 -->
        <attr name="cmlv_rail_count" format="integer" />
        <!-- 音轨的颜色 -->
        <attr name="cmlv_rail_color" format="color" />
        <!-- 音轨线宽粗细 -->
        <attr name="cmlv_line_width" format="integer|float|dimension" />
    </declare-styleable>

    <declare-styleable name="RestartView">
        <!-- 线的数量 -->
        <attr name="rtv_line_count" format="integer" />
        <!-- 背景的圆的颜色 -->
        <attr name="rtv_bg_circle_color" format="color" />
        <!-- 线的颜色 -->
        <attr name="rtv_line_color" format="color" />
        <!-- 线的线宽粗细 -->
        <attr name="rtv_line_width" format="integer|float|dimension" />
    </declare-styleable>

    <declare-styleable name="ShutdownView">
        <!-- 背景的圆的颜色 -->
        <attr name="sdv_bg_circle_color" format="color" />
        <!-- 线的颜色 -->
        <attr name="sdv_line_color" format="color" />
        <!-- 线的线宽粗细 -->
        <attr name="sdv_line_width" format="integer|float|dimension" />
    </declare-styleable>

    <declare-styleable name="ShareView">
        <!-- 颜色 -->
        <attr name="sv_color" format="color" />
        <!-- 小圆内部的填充颜色 -->
        <attr name="sv_small_circle_solid_color" format="color" />
        <!-- 线宽粗细 -->
        <attr name="sv_line_width" format="integer|float|dimension" />
    </declare-styleable>

    <declare-styleable name="PolygonLoadingView">
        <!-- 开始颜色 -->
        <attr name="plv_start_color" format="color|reference" />
        <!-- 结束颜色 -->
        <attr name="plv_end_color" format="color|reference" />
        <!-- 多边形多少条边，至少3条边，如果小于3条，会强制3条 -->
        <attr name="plv_num" format="integer" />
        <!-- 多边形有多少层 -->
        <attr name="plv_layer_count" format="integer" />
    </declare-styleable>

    <declare-styleable name="AlipayRingLoadingView">
        <!-- 圆弧的颜色 -->
        <attr name="arlv_arc_color" format="color" />
        <!-- 背景圆的颜色 -->
        <attr name="arlv_bg_circle_color" format="color" />
    </declare-styleable>

    <declare-styleable name="StarsView">
        <!-- 星星的颜色 -->
        <attr name="stv_color" format="color" />
        <!-- 星星的边数 -->
        <attr name="stv_num" format="integer|dimension|reference" />
        <!-- 边的线宽 -->
        <attr name="stv_edge_line_width" format="float|dimension|reference" />
        <!-- 填充风格 -->
        <attr name="stv_style" format="enum">
            <!-- 填满 -->
            <enum name="fill" value="1" />
            <!-- 描边 -->
            <enum name="stroke" value="2" />
        </attr>
    </declare-styleable>

    <declare-styleable name="DownloadEntryView">
        <!-- 颜色 -->
        <attr name="dev_color" format="color" />
        <!-- 线宽 -->
        <attr name="dev_line_width" format="float|dimension|reference" />
    </declare-styleable>

    <declare-styleable name="PolygonSettingView">
        <!-- 开始颜色 -->
        <attr name="psv_color" format="color|reference" />
        <!-- 多边形多少条边，至少3条边，如果小于3条，会强制3条 -->
        <attr name="psv_num" format="integer" />
        <!-- 边的线宽 -->
        <attr name="psv_line_width" format="float|dimension|reference" />
    </declare-styleable>

    <declare-styleable name="HookCheckBox">
        <!-- 选中时圆的颜色 -->
        <attr name="hcb_check_circle_color" format="color" />
        <!-- 未选中时圆的颜色 -->
        <attr name="hcb_uncheck_circle_color" format="color" />
        <!-- 选中时的钩子的颜色 -->
        <attr name="hcb_check_hook_color" format="color" />
        <!-- 未选中时的钩子的颜色 -->
        <attr name="hcb_uncheck_hook_color" format="color" />
        <!-- 是否选中 -->
        <attr name="hcb_is_check" format="boolean" />
        <!-- 风格 -->
        <attr name="hcb_style" format="enum">
            <!-- 普通，对勾是有颜色的 -->
            <enum name="normal" value="1" />
            <!-- 镂空，就是对勾是透明的 -->
            <enum name="hollow_out" value="2" />
        </attr>
        <!-- 线宽 -->
        <attr name="hcb_line_width" format="float|dimension|reference" />
    </declare-styleable>

    <declare-styleable name="TIMServiceNoView">
        <!-- 背景圆的颜色 -->
        <attr name="tim_snv_bg_circle_color" format="color" />
        <!-- 圆角矩形图标的颜色 -->
        <attr name="tim_snv_round_rect_icon_color" format="color" />
    </declare-styleable>
</resources>